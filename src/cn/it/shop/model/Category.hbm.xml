<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">
<!-- 
    Mapping file autogenerated by MyEclipse Persistence Tools
-->
<hibernate-mapping package="cn.it.shop.model">
    <class name="Category" table="category" catalog="shop">
        <id name="id" type="java.lang.Integer">
            <column name="id" />
            <generator class="native" />
        </id>
        <property name="type" type="java.lang.String">
            <column name="type" length="20" />
        </property>
        <property name="hot" type="java.lang.Boolean">
            <column name="hot" />
        </property>
        
        <!-- lazy="false" 则级联查询关联对象
        	N + 1: 首先发出一条语句查询当前对象，然后发出N条语句查询关联对象，N不确定(N是所有的category中的aid的种类数)
        	lazy: 是否加载关联对象，如果为false，则即使加载
        	fetch: 什么样的方式加载关联对象，可以选择select(默认)和join
        	注意: 在many-to-one的情况下，join是无效的(只适合one-to-many)，即使有效也不要配置前两个参数，因为配置在xml中不灵活
        	解决方案:自己写HQL语句取代基于XML的配置，则可以提升性能，在任何情况下，不需要配置lazy="false", fetch="join"
         -->
        <many-to-one name="account" class="Account">
        	<column name="aid" />
        </many-to-one>
    </class>
</hibernate-mapping>
